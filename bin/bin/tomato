#!/bin/sh

duration=20m
if [ $# -eq 1 ]; then
  duration=$1
fi

termdown $duration 
if [ "$(uname)" == "Darwin" ]; then
  osascript -e 'display alert "Tomato up!" message "🍅"'
else
  echo "🍅" >/tmp/tomato
  $TERM -e less /tmp/tomato
fi
